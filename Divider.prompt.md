A barely-there separator between sections or list rows.

```jsx
<Divider />                    {/* rose gradient, fades at both ends */}
<Divider variant="hair" />     {/* flat hairline */}
<Divider variant="on-dark" />  {/* on charcoal surfaces */}
```

Use the gradient variant between full-width page sections; use hair/rule inside cards and lists.
